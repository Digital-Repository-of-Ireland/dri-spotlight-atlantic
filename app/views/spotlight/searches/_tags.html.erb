<% if tags.any? %>
<div class="row">
  <div class="col-md-12 text-center">
    <ul class="nav nav-pills tags">
      <li class="<%= 'active' if params[:tag].blank? %>"><%= link_to t('.all'), exhibit_home_page_path %></li>

      <% tags.sort_by(&:name).each do |tag| %>
        <li class="<%= 'active' if params[:tag] == tag.name %>"><%= link_to tag.name, exhibit_home_page_path(tag: tag.name) %></li>
      <% end %>
    </ul>
  </div>
</div>
<% end %>
